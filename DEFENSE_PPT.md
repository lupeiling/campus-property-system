# 高校后勤物业管理系统毕业设计答辩PPT

---

## 封面页

**高校后勤物业管理系统**

**设计与实现**

答辩人：XXX
指导教师：XXX
专业：XXX
日期：2026年X月

---

## 第1页 — 目录

1. 课题背景与意义
2. 系统需求分析
3. 技术架构设计
4. 核心功能模块
5. 数据库设计
6. 核心流程实现
7. 系统展示
8. 总结与展望

---

## 第2页 — 课题背景与意义

### 课题背景

- 高校规模持续扩大，后勤管理复杂度急剧增长
- 传统的纸质审批、电话报修方式效率低下，响应不及时
- 宿舍分配、设施维修、物资采购等环节存在信息孤岛
- 多类用户（学生、教师、后勤人员、管理员）需求各异，缺乏统一入口

### 课题意义

- 构建数字化、流程化的后勤物业管理平台
- 打通"报修—派工—维修—评价"全链路闭环
- 实现物资"采购—入库—领用—归还"全生命周期管理
- 连接学生、教师、后勤、管理员四种角色，一站式服务

---

## 第3页 — 系统需求分析

### 用户角色分析

| 角色 | 核心需求 |
|------|---------|
| **学生(STUDENT)** | 宿舍报修、调宿/退宿申请、消费记录查看、费用查询 |
| **教师(TEACHER)** | 住房待遇管理、住房费用查询、设施报修 |
| **后勤人员(STAFF)** | 工单审核派工、设施管理、物资采购管理、清洁排班、宿舍审批 |
| **管理员(ADMIN)** | 用户管理、角色管理、系统日志查看、全部功能访问 |

### 功能需求概述

- 用户认证与权限控制（登录/注册/角色鉴权）
- 宿舍楼与房间管理（新增/分配/调宿/退宿/审批）
- 统一报修工单系统（提交→审核→派工→维修→完成→评价）
- 物资与库存管理（物料分类/采购审批/领用归还/库存预警）
- 食堂管理（食堂信息/菜品/消费记录/卫生检查）
- 设施管理（公共设施台账/状态监控/设施报修）
- 人员管理（后勤人员聘用/宿管排班/清洁任务分配）
- 数据分析与统计面板
- 数据导出（Excel/PDF）

---

## 第4页 — 技术架构设计

### 整体架构

```
┌─────────────────────────────────────────────────┐
│                  前端展示层 (Vue 2)               │
│   Element UI  │  Axios  │  Vue Router  │  Vuex   │
├─────────────────────────────────────────────────┤
│                HTTP RESTful API                  │
│     Bearer Token (JWT)  │  JSON 数据交互         │
├─────────────────────────────────────────────────┤
│                后端服务层 (Spring Boot 2.7)       │
│  Spring Security │ JWT Filter │ Controller       │
│  Service Layer  │  MyBatis-Plus  │  Mapper       │
├─────────────────────────────────────────────────┤
│                数据存储层 (MySQL 8.0)             │
│   28张业务表  │  逻辑删除  │  自动填充时间戳       │
└─────────────────────────────────────────────────┘
```

### 技术选型

| 层级 | 技术 | 版本 |
|------|------|------|
| 后端框架 | Spring Boot | 2.7.18 |
| 安全框架 | Spring Security + JWT | jjwt 0.11.5 |
| ORM框架 | MyBatis-Plus | 3.5.3.1 |
| 数据库 | MySQL | 8.0 |
| 前端框架 | Vue.js | 2.x |
| UI组件库 | Element UI | - |
| 状态管理 | Vuex | - |
| 导出工具 | Apache POI / OpenPDF | 5.2.4 / 1.3.30 |
| 工具库 | Hutool | 5.8.20 |
| 构建工具 | Maven | - |

---

## 第5页 — 系统功能模块总览

```
高校后勤物业管理系统
├── 1. 系统管理模块
│   ├── 用户管理（CRUD + 角色分配）
│   ├── 角色权限管理（ROLE_ADMIN / STAFF / TEACHER / STUDENT）
│   └── 系统操作日志（审计追踪）
├── 2. 宿舍管理模块
│   ├── 宿舍楼管理（楼栋信息 CRUD）
│   ├── 房间管理（房间信息、容量、状态）
│   ├── 宿舍分配（学生入住）
│   ├── 调剂申请（学生调宿）
│   ├── 退宿申请（学生退宿）
│   ├── 审批管理（STAFF 审核）
│   └── 宿管排班（值班安排）
├── 3. 报修管理模块
│   ├── 报修工单（统一受理宿舍+设施报修）
│   ├── 审核派工（分配维修人员）
│   ├── 维修执行（开始→完成→结果记录）
│   └── 服务评价（评分+评价内容）
├── 4. 物资管理模块
│   ├── 物资分类管理
│   ├── 物资库存（台账、库存预警）
│   ├── 采购管理（申请→审批→入库）
│   ├── 领用记录（出库登记）
│   └── 归还管理（退料入库）
├── 5. 食堂管理模块
│   ├── 食堂信息管理
│   ├── 菜品管理
│   ├── 消费记录
│   └── 卫生检查
├── 6. 设施管理模块
│   ├── 设施台账
│   ├── 设施状态监控
│   └── 设施报修
├── 7. 人员管理模块
│   ├── 人员聘用
│   └── 清洁排班
├── 8. 费用管理模块
│   ├── 学生费用事项
│   └── 教师住房费用
├── 9. 反馈管理模块
│   └── "我有话说"留言反馈
└── 10. 统计分析模块
    └── Dashboard 数据面板
```

---

## 第6页 — 数据库设计（ER关系）

### 核心实体关系

```
  sys_role (角色)
     │
     └──── sys_user (用户)
              │
              ├── repair_order (报修工单)
              │     └── repair_evaluation (维修评价)
              │
              ├── dorm_allocation (宿舍分配)
              │     └── dorm_room (房间)
              │           └── dorm_building (宿舍楼)
              │
              ├── material_usage (物资领用)
              │     └── material (物资)
              │           └── material_category (物资分类)
              │
              ├── material_purchase (采购申请)
              │     └── material_purchase_item (采购明细)
              │
              ├── feedback (留言反馈)
              ├── student_fee (学生费用)
              ├── teacher_housing (教师住房)
              └── sys_log (操作日志)
```

### 设计特点
- 主键采用自增 Long 型 ID
- 统一使用逻辑删除（deleted 字段）
- 自动填充 createTime / updateTime
- 使用 @TableField 和 @TableLogic 注解
- 一对一/一对多关系通过外键维护

---

## 第7页 — 核心功能一：报修处理全流程

### 工单状态机

```
                    ┌────────────────────────────────────────┐
                    │           报修工单生命周期               │
                    └────────────────────────────────────────┘

  [用户提交] ──→ 状态0:待审核 ──→ 状态5:已拒绝(终态)
                    │
                    ↓ (管理员/STAFF审核通过)
             状态1:已审核 ──→ (STAFF指派维修人员)
                    │
                    ↓
             状态2:已派工 ──→ (维修人员开始工作)
                    │
                    ↓
             状态3:维修中 ──→ (维修完成,填写结果)
                    │
                    ↓
             状态4:已完成 ──→ (用户评价)
                    │
                    ↓
              评价完成 (终态)
```

### 关键技术点

- 工单号生成: `"RO" + yyyyMMddHHmmss`（全局唯一）
- 来源类型分类: 宿舍(sourceType=1) / 设施(sourceType=2)
- 维修人员通过 `repairPersonId` 关联用户表
- 评价表与工单表通过 `orderId` 一对一关联
- 按角色区分可执行操作（通过 `@PreAuthorize` 控制权限）

---

## 第8页 — 核心功能二：物资库存管理流程

### 物资全生命周期

```
  ┌──────────────┐     ┌──────────────┐     ┌──────────────┐
  │   物资分类   │ ──→ │   物资台账   │ ──→ │   库存预警   │
  │  (category)  │     │  (material)  │     │ (minQuantity)│
  └──────────────┘     └──────────────┘     └──────────────┘
                              │
          ┌───────────────────┼───────────────────┐
          ↓                   ↓                    ↓
  ┌──────────────┐   ┌──────────────┐    ┌──────────────┐
  │   采购入库   │   │   物资领用   │    │   物资归还   │
  │  (purchase)  │   │   (usage)    │    │  (return)    │
  └──────────────┘   └──────────────┘    └──────────────┘
```

### 采购审批流程

1. **提交申请**: STAFF 提交 `PurchaseApplyDTO`（含多项采购明细）
2. **生成采购单**: 自动生成采购编号 `"PO" + yyyyMMddHHmmss`，计算总金额
3. **管理员审批**: 审批通过(status=1)时，自动将采购物资数量加入库存
4. **拒绝**: 审批拒绝(status=2)时，仅更新状态

### 领用归还流程

- **领用**: 扣减库存 → 创建领用记录(status=1:已领用)
- **归还**: 恢复库存 → 更新领用记录(status=0:已归还)
- **库存不足**: 抛出 `RuntimeException("库存不足")`
- 所有操作均使用 `@Transactional` 确保数据一致性

### 关键代码逻辑

```java
// 采购审批通过时自动入库
if (status == 1) {
    for (MaterialPurchaseItem item : items) {
        Material material = materialMapper.selectById(item.getMaterialId());
        material.setStockQuantity(material.getStockQuantity() + item.getQuantity());
        materialMapper.updateById(material);
    }
}

// 领用时库存校验
if (material.getStockQuantity() < quantity) {
    throw new RuntimeException("库存不足");
}
material.setStockQuantity(material.getStockQuantity() - quantity);
```

---

## 第9页 — 安全认证机制

### JWT 无状态认证流程

```
  ┌─────────┐                  ┌──────────┐              ┌──────────┐
  │  Vue前端 │                  │ 后端服务  │              │   MySQL  │
  └────┬────┘                  └────┬─────┘              └────┬─────┘
       │                            │                         │
       │ POST /api/auth/login       │                         │
       │ {username, password}       │                         │
       │───────────────────────────→│                         │
       │                            │ 查询用户                │
       │                            │────────────────────────→│
       │                            │←────────────────────────│
       │                            │ 验证密码 + 状态         │
       │                            │ 生成 JWT Token          │
       │ ←──────────────────────────│                         │
       │ {code:200, data:{user,token}}                       │
       │                            │                         │
       │ 存储 token 到 localStorage  │                         │
       │                            │                         │
       │ 后续请求携带                │                         │
       │ Authorization: Bearer xxx  │                         │
       │───────────────────────────→│                         │
       │                            │ JwtAuthenticationFilter │
       │                            │ 解析Token → 设置上下文   │
       │                            │ @PreAuthorize 权限校验    │
```

### 安全设计要点

1. **JWT Token**: HS256 签名，包含 username + role，支持过期配置
2. **Filter 拦截**: `JwtAuthenticationFilter` 继承 `OncePerRequestFilter`，每次请求自动解析 Token
3. **权限控制**: Controller 层通过 `@PreAuthorize("hasRole('ADMIN')")` 注解控制
4. **路由守卫**: Vue Router `beforeEach` 钩子校验 Token 和角色
5. **401/403 响应**: 前端拦截器自动处理 Token 过期和权限不足
6. **密码安全**: 使用 Salt 加密存储（CustomUserDetailsService 校验用户状态）

---

## 第10页 — 前端架构设计

### 项目结构

```
campus-property-web/src/
├── api/                    # API 接口层 (按模块拆分)
│   ├── user.js             #   用户相关接口
│   ├── repair.js           #   报修相关接口
│   ├── material.js         #   物资相关接口
│   ├── dorm.js             #   宿舍相关接口
│   ├── canteen.js          #   食堂相关接口
│   ├── facility.js         #   设施相关接口
│   ├── system.js           #   系统管理接口
│   ├── export.js           #   数据导出接口
│   └── ...                 #   其他接口
├── router/index.js         # 路由配置 (30+路由，含角色守卫)
├── store/index.js          # Vuex 状态管理 (token/user/role)
├── utils/request.js        # Axios 封装 (拦截器统一处理)
├── views/                  # 页面组件
│   ├── Login.vue           #   登录页
│   ├── Register.vue        #   注册页
│   ├── Layout.vue          #   主布局
│   ├── Dashboard.vue       #   统计分析面板
│   ├── ServiceHall.vue     #   办事大厅（首页）
│   ├── repair/             #   报修模块
│   ├── dorm/               #   宿舍模块
│   ├── material/           #   物资模块
│   ├── canteen/            #   食堂模块
│   ├── facility/           #   设施模块
│   ├── staff/              #   人员管理模块
│   ├── system/             #   系统管理模块
│   └── ...
└── App.vue / main.js       # 应用入口
```

### 关键设计

- **API 层**: 每个业务模块一个 JS 文件，统一使用封装的 `request` 实例
- **路由守卫**: 按角色控制页面访问（`meta.role`），未登录跳转 `/login`
- **状态管理**: Vuex 集中管理 Token 和用户信息，持久化到 localStorage
- **拦截器**: 自动注入 Bearer Token，统一处理 401/403 错误
- **权限控制**: 计算属性 `isStaff`/`isAdmin` 控制 UI 元素显隐
- **组件按需加载**: 路由懒加载 (`() => import(...)`)

---

## 第11页 — 系统亮点与技术特色

### 1. 前后端分离架构
- 前端 Vue SPA + 后端 RESTful API，松耦合、可独立部署
- Axios 拦截器统一处理认证和错误

### 2. JWT 无状态认证
- 服务端不存 Session，水平扩展友好
- Token 携带角色信息，前后端双重权限校验

### 3. MyBatis-Plus 持久层
- Lambda 表达式构建查询，避免字段名硬编码
- 逻辑删除 + 自动填充，减少样板代码
- 分页插件内置支持

### 4. 工单状态机设计
- 报修工单 6 种状态流转，业务清晰可控
- 统一工单受理宿舍和设施两类报修来源

### 5. 事务保障数据一致性
- 采购审批通过 → 自动入库
- 物资领用 → 库存扣减 → 领用记录创建
- 全部使用 `@Transactional` 原子操作

### 6. 多角色多权限
- 4 种角色（ADMIN/STAFF/TEACHER/STUDENT）
- 后端 `@PreAuthorize` + 前端 `router.beforeEach` + UI 条件渲染
- 三层权限控制

### 7. 数据导出能力
- Apache POI 实现 Excel 导出
- OpenPDF 实现 PDF 导出
- 维修工单统计表支持两种格式

---

## 第12页 — 系统展示（关键页面截屏描述）

### 页面清单

| 序号 | 页面名称 | 功能描述 |
|------|----------|----------|
| 1 | 登录/注册页 | 用户名密码登录 + 新用户注册 |
| 2 | 办事大厅（首页） | 功能入口导航（学生端） |
| 3 | 统计分析面板 | 数据总览图表（STAFF/ADMIN端） |
| 4 | 维修工单管理 | 工单列表 + 状态筛选 + 全流程操作按钮 |
| 5 | 宿舍房间管理 | 楼栋管理 + 房间列表 + 状态监控 |
| 6 | 宿舍分配管理 | 学生入住分配 |
| 7 | 物资库存管理 | 物资列表 + 分类筛选 + 增删改查 |
| 8 | 采购管理 | 采购申请 + 审批流程 |
| 9 | 领用记录 | 物资出库 + 归还管理 |
| 10 | 食堂管理 | 食堂/菜品/消费/检查 |
| 11 | 设施管理 | 设施台账 + 设施报修 |
| 12 | 用户管理 | 用户CRUD + 角色分配（ADMIN） |
| 13 | 反馈管理 | 留言提交 + STAFF回复 |
| 14 | 系统日志 | 操作日志查看（ADMIN） |

---

## 第13页 — 总结与展望

### 已完成工作

1. 完成了28张业务数据表的设计与实现
2. 实现了11个后端 Controller、5个 Service 业务逻辑层
3. 完成了4种用户角色的权限体系
4. 实现了报修工单全生命周期管理（提交→审核→派工→维修→完成→评价）
5. 实现了物资采购-入库-领用-归还的完整链路
6. 实现了宿舍、食堂、设施、人员、费用等多维度管理
7. 前端实现了30+页面的 Vue SPA 应用
8. 支持数据导出（Excel/PDF）

### 存在的不足

- 缺乏移动端适配
- 消息通知机制不完善（无实时推送）
- 物资库存预警仅有阈值字段，无自动告警
- 缺少接口文档自动化生成（如 Swagger）

### 未来展望

- 接入微信小程序或移动端 APP
- 引入 WebSocket 实现消息实时推送
- 集成智能巡检（IoT传感器 + AI分析）
- 引入工作流引擎（如 Flowable）实现可配置审批流程
- 增加数据大屏可视化

---

## 第14页 — 致谢

**感谢各位老师批评指正！**

**请各位老师提问**

---

# 答辩汇报讲稿

## 开场白 (1分钟)

各位老师好！我是XXX，我的毕业设计题目是"高校后勤物业管理系统的设计与实现"。下面我将从课题背景、系统架构、核心功能和实现过程等几个方面向各位老师汇报我的毕业设计成果。

## 一、课题背景与意义 (1.5分钟)

随着我国高等教育事业的快速发展，高校在校生规模不断扩大，这给高校后勤物业管理带来了巨大的挑战。以前，很多高校的后勤管理还停留在纸质审批、电话报修的传统模式，存在报修响应慢、物资管理混乱、信息孤岛等问题。

比如，一个学生宿舍的水管坏了，他需要先打电话给宿管，宿管再打电话给维修师傅，整个流程没有记录、没有跟踪、没有反馈，效率非常低。同样，后勤部门在管理物资时，采购了多少、库存还有多少、谁领用了什么，可能都记在纸上或Excel里，很难做到精确管理。

基于这些痛点，我设计并实现了一套基于Web的高校后勤物业管理系统。这套系统的核心目标就是：将高校后勤管理全面数字化、流程化，连接学生、教师、后勤人员和管理员四类用户，提供一站式的后勤服务。

## 二、系统需求与角色设计 (1分钟)

在需求分析阶段，我明确了系统的四类用户角色及其核心需求：

学生端需要宿舍报修、调宿退宿申请、消费记录查看等功能；教师端需要住房待遇管理和设施报修；后勤人员端是系统的核心操作者，负责工单审核派工、物资采购管理、宿舍审批、清洁排班等；而管理员端拥有最高权限，可以管理用户、配置角色和查看系统日志。

基于这个需求分析，我划分了十个核心功能模块。

## 三、技术架构 (1.5分钟)

在技术选型上，我采用了目前企业开发中主流的**前后端分离架构**。

后端基于 **Spring Boot 2.7** 框架，配合 **MyBatis-Plus** 作为持久层框架。MyBatis-Plus对MyBatis进行了增强，提供了Lambda表达式查询、自动分页、逻辑删除、自动填充等功能，大大减少了SQL编写的重复工作。安全方面采用 **Spring Security + JWT** 实现无状态认证。JWT即JSON Web Token，用户登录后服务端返回一个加密的Token，之后的每次请求只需携带这个Token即可完成身份识别，服务端不需要维护Session，系统可以方便地进行水平扩展。

前端采用 **Vue 2** 框架配合 **Element UI** 组件库。Element UI是饿了么开源的企业级UI组件库，提供了表格、表单、对话框等丰富的组件，能够快速构建美观的管理后台界面。数据交互使用Axios，通过拦截器统一处理Token注入和错误响应。状态管理使用Vuex，将用户信息和Token集中管理。

数据库采用 **MySQL 8.0**，共设计了28张业务数据表。

## 四、核心功能实现 (3分钟)

接下来我重点汇报两个核心功能的实现过程。

### （一）报修工单全流程管理

这是整个系统最核心的业务功能，我设计了一个**工单状态机**来管理报修工单的整个生命周期。

用户提交报修后，工单进入"待审核"状态。后勤人员审核通过后，工单变为"已审核"状态，然后指派维修人员，进入"已派工"状态。维修人员到现场后点击开始，进入"维修中"状态；维修完成后填写维修结果，变为"已完成"。最后，用户可以对该次维修服务进行星级评分和文字评价。如果审核不通过，工单会进入"已拒绝"终态。

以维修工单管理页面为例，我使用Vue实现了表格展示和数据筛选功能。表格上方提供了状态下拉筛选和来源类型筛选，可以快速定位工单。操作列根据工单当前状态动态显示对应的操作按钮：状态为待审核时显示"审核"和"拒绝"按钮，已审核时显示"派工"按钮，已派工时显示"开始维修"，维修中显示"完成"，完成后显示"评价"。同时，这些操作按钮也会根据用户角色进行权限控制，普通学生只能看到自己提交的工单和评价按钮。

后端RepairController中，通过RESTful风格的API提供了7个接口：分页列表查询、单个查询、提交工单、审核、派工、开始维修、完成维修、提交评价。每个状态变更接口都使用`@PreAuthorize`注解限制只有管理员和后勤人员才能操作，确保了业务操作的权限安全。

### （二）物资库存管理

物资管理的核心是库存的精确管控，我实现了"采购—入库—领用—归还"的完整链路。

在采购环节，后勤人员可以提交采购申请单，一个采购单可以包含多个物资明细项。管理员审批通过后，系统会自动将采购数量加入对应物资的库存。这里我使用了Spring的`@Transactional`事务注解，确保采购单状态更新和库存增加这两个操作要么同时成功、要么同时回滚，保证数据一致性。

在领用环节，后勤人员填写领用的物资ID、数量和用途，系统首先校验库存是否充足。如果库存不足，直接抛出异常提示；库存充足则扣减库存并创建领用记录。归还时，系统恢复库存并将领用记录标记为已归还。

这个模块一共涉及5张数据表：物资分类表、物资表、采购申请表、采购明细表、领用记录表，形成了一个完整的物资管理数据体系。

## 五、系统展示 (1分钟)

接下来我为各位老师快速展示几个核心页面。

首先登录系统，使用学生账号登录后进入办事大厅首页，可以看到功能导航入口。点击"宿舍报修"进入报修页面，可以提交新的报修工单并查看历史记录。切换到管理员账号登录，可以看到统计分析面板，展示了各类数据的汇总统计。进入维修管理页面，可以看到所有工单列表，管理员可以执行审核、派工等操作。再进入物资管理页面，可以查看库存状态、处理采购审批和领用记录。

## 六、总结与展望 (1分钟)

总结来说，本系统完成了以下工作：第一，设计并实现了28张业务数据表；第二，完成了11个后端Controller和5个核心Service；第三，实现了4种角色的完整权限体系；第四，核心实现了报修工单全生命周期管理和物资库存管理两大业务流程；第五，前端实现了30多个页面的完整应用。

当然，本系统也存在一些不足之处，比如缺乏移动端适配、没有消息实时推送功能、物资库存预警目前只设置了阈值字段但没有自动告警机制、缺少Swagger接口文档等。

未来可以从以下几个方面进行改进和扩展：接入微信小程序或开发移动端APP，让师生可以随时随地使用系统；引入WebSocket实现维修进度、审批结果等消息的实时推送；结合IoT传感器实现设备智能巡检和自动报修；引入工作流引擎实现可配置的审批流程，提高系统灵活性。

## 结束语 (30秒)

以上就是我的毕业设计汇报，感谢各位老师的聆听。接下来请各位老师提问，我将认真回答。

---

## 可能被问到的常见问题及回答要点

### Q1: 为什么选择Spring Boot而不是其他框架？
A: Spring Boot是目前Java生态中最主流的企业级开发框架，它简化了Spring应用的搭建和配置过程，内置了Tomcat服务器，开箱即用。结合MyBatis-Plus，可以用很少的代码完成复杂的数据库操作。对于毕业设计来说，Spring Boot生态成熟、文档丰富、社区活跃，开发效率高。

### Q2: 为什么使用JWT而不是传统的Session？
A: JWT是无状态的，服务端不需要存储Session信息，这有利于系统的水平扩展。同时，前后端分离架构中，前端是独立的Vue应用，不依赖服务端的Cookie机制，JWT通过HTTP Header传输，天然适配前后端分离。我在Token中编码了用户角色信息，后端Filter解析后可以直接用于权限判断，无需每次请求都查询数据库。

### Q3: 系统如何保证数据安全性？
A: 第一，用户密码通过盐值加密存储，不可逆。第二，JWT Token有过期时间，防止Token泄漏后的长期风险。第三，前端使用路由守卫，后端使用`@PreAuthorize`注解，双重权限控制。第四，前端发送请求时通过Axios拦截器自动添加Token，服务端通过JwtAuthenticationFilter从Authorization请求头中提取Token并校验。

### Q4: 报修流程中的状态机是如何实现的？
A: 工单状态存储在数据库的status字段中，使用整型定义6种状态（0-5）。工单的状态流转通过在Controller中提供对应方法来控制。比如审核通过时调用approveOrder方法，将status从0更新为1。每个状态流转方法都通过`@PreAuthorize`注解进行权限验证。前端的操作按钮通过计算当前工单的status值和用户角色来动态显示对应按钮。

### Q5: 系统如何支持水平扩展？
A: 由于前后端完全分离，前端可以单独部署到Nginx或CDN上，后端可以部署多个实例通过负载均衡分发请求。认证使用JWT无状态方案，任何后端实例都可以独立验证Token，不需要共享Session。这样当访问量增大时，只需增加后端实例即可。

### Q6: 数据库为什么使用逻辑删除而不是物理删除？
A: 使用MyBatis-Plus提供的`@TableLogic`注解实现逻辑删除，删除操作实际上是将deleted字段标记为1。这样做的好处是：数据不会真正丢失，便于数据恢复和审计追踪；查询时MyBatis-Plus会自动在SQL中拼接`deleted=0`的条件，对业务代码无侵入。
